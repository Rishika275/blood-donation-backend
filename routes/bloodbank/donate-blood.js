var router = require('express').Router();
var connection = require('../../config/databases')

//give blood to the requester
router.post('/', (req, res, next) => {
	connection.query(
		
	)
}
)

module.exports = router;